function addActiveClassToNuevaPartidaMenu() {
    // Select the nuevaPartidaMenu element
    const nuevaPartidaMenu = document.getElementById('nuevaPartidaMenu');

    // Add the "active" class
    nuevaPartidaMenu.classList.add('active');
}
function addActiveClassTomiPerfilMenu() {
    // Select the miPerfilMenu element
    const miPerfilMenu = document.getElementById('miPerfilMenu');

    // Add the "active" class
    miPerfilMenu.classList.add('active');
}
function addActiveClassToverRankingMenu() {
    // Select the verRankingMenu element
    const verRankingMenu = document.getElementById('verRankingMenu');

    // Add the "active" class
    verRankingMenu.classList.add('active');
}
function addActiveClassTohistorialDePartidasMenu() {
    // Select the historialDePartidasMenu element
    const historialDePartidasMenu = document.getElementById('historialDePartidasMenu');

    // Add the "active" class
    historialDePartidasMenu.classList.add('active');
}
function addActiveClassTosugerirPreguntaMenu() {
    // Select the sugerirPreguntaMenu element
    const sugerirPreguntaMenu = document.getElementById('sugerirPreguntaMenu');

    // Add the "active" class
    sugerirPreguntaMenu.classList.add('active');
}
